name: nsa_diff
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch>=2.1.0
      - torchvision>=0.16.0
      - diffusers>=0.25.0
      - accelerate>=0.25.0
      - tensorly>=0.8.1
      - pytorch-fid>=0.3.0
      - wandb>=0.16.0
      - pytest>=7.4.0
      - pytest-cov>=4.1.0
      - safetensors>=0.4.0
